@using System.Threading.Tasks
@using ProjectPaula.Model.CalendarExport
<div id="datesDialog" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Überschneidungen</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <div class="modal-body">

                <p>Hier siehst du im Detail, welche Kurse an welchen Terminen Überschneidungen mit <strong>{{vm.props.SelectedCourse.Title}}</strong> verursachen.</p>

                <div class="container" style="width: 100%">
                    <div class="table-responsive">
                        <table class="table">
                            <thead>
                                <tr>
                                    <th>Termine</th>
                                    <th ng-repeat="courseName in vm.props.DatesDialogContent.CourseNames">{{courseName}}</th>
                                </tr>
                            </thead>
                            <tbody>
                                <tr ng-repeat="overlap in vm.props.DatesDialogContent.Overlaps">
                                    <td style="min-width: 200px;">{{overlap.Key}}</td>
                                    <td ng-repeat="time in overlap.Value track by $index">{{time}}</td>
                                </tr>
                            </tbody>
                        </table>
                    </div>
                </div>

            </div>
            <div class="modal-footer">
                <button type="button" class="btn btn-primary" data-dismiss="modal">Schließen</button>
            </div>
        </div>
    </div>
</div>