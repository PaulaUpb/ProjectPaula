<div id="searchCourseModal" class="modal fade">
    <div class="modal-dialog">
        <div class="modal-content">
            <div class="modal-header">
                <h4 class="modal-title">Veranstaltungen suchen</h4>
                <button type="button" class="close" data-dismiss="modal" aria-label="Close"><span aria-hidden="true">&times;</span></button>
            </div>
            <div class="modal-body">
                <p>Finde Veranstaltungen aus dem PAUL-Vorlesungsverzeichnis anhand des Namens, der Abkürzung, oder des Dozenten und füge sie deinem Stundenplan hinzu.</p>
                <p class="error">{{vm.sync.User.Errors.CourseSearchMessage}}</p>
                
                <input type="text" ng-model="vm.props.SearchQuery" paula-enter="searchCourses(vm.props.SearchQuery)" placeholder="Suchbegriff" focus-on="searchInput"/>
                <button type="button" class="btn" ng-click="searchCourses(vm.props.SearchQuery)">Suchen</button>
                <ul class="listView">
                    <li class="listView-item" ng-repeat="result in vm.sync.Search.SearchResults">

                        <div class="listView-item-info">
                            <p class="listView-item-title">{{result.MainCourse.Name}}</p>
                            <p class="listView-item-subtitle">{{result.MainCourse.Time}}</p>
                            <p class="listView-item-subsubtitle">{{result.MainCourse.ShortName}}</p>
                            <p class="listView-item-subsubtitle">{{result.MainCourse.Docent}}</p>

                            <div ng-repeat="course in result.ConnectedCourses" style="margin-top: 8px;">
                                <p class="listView-item-title">{{course.Name}}</p>
                                <p class="listView-item-subtitle">{{course.Time}}</p>
                                <p class="listView-item-subsubtitle">{{course.ShortName}}</p>
                                <p class="listView-item-subsubtitle">{{course.Docent}}</p>
                            </div>
                        </div>

                        <div class="listView-item-commands">
                            <button ng-if="!result.MainCourse.IsAdded && result.MainCourse.Time"  type="button" ng-click="addCourse(result.MainCourse.Id)" data-course-id="{{result.MainCourse.Id}}" class="btn btn-default"><span class="glyphicon glyphicon-plus"></span> Hinzufügen</button>
                            <button ng-if="result.MainCourse.IsAdded && result.MainCourse.Time"   type="button" ng-click="removeCourse(result.MainCourse.Id)" data-course-id="{{result.MainCourse.Id}}" class="btn btn-default"> <span class="glyphicon glyphicon-minus"></span> Entfernen</button>
                        </div>
                    </li>
                </ul>
            </div>
        </div><!-- /.modal-content -->
    </div><!-- /.modal-dialog -->
</div>